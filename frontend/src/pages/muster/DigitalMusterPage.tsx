import { MusterBoard } from '../../features/muster/components/dnd/MusterBoard';
import { Box } from '@mui/material';

export const DigitalMusterPage = () => {
    return (
        <Box sx={{ p: 0 }}>
            <MusterBoard />
        </Box>
    );
};
